# 邪修交通 · 系统架构设计 v1.0

| 项 | 值 |
|----|-----|
| **版本** | v1.0 |
| **日期** | 2026-09-23（Asia/Shanghai） |
| **基础框架** | 自研规则引擎（Node CJS：`engine/pipeline.js`）+ 微信小程序（Vant Weapp） |
| **核心技术栈摘要** | 小程序 `@vant/weapp`；引擎 Node.js（`mct` / `score` / `pipeline` / `adapters`）；数据 `Leg` + `TransferPlay` JSON Schema；MVP 纯 mock 可演示 |
| **产品红线** | **只推荐不卖票**；**程序算计划，禁止 LLM 编造车次与票价**；**密钥不进仓；禁止违规爬取** |
| **输入依据** | `docs/产品需求整理.md`、`docs/技术与设计拍板.md`、`docs/data-sources.md`、`docs/data-contract-mock.md`、`docs/hubs-mvp.md`、`docs/api-plans-contract.md`、`engine/`、`miniprogram/` |

> 方法论遵循 `docs/skills/arch-design-doc.md`（七律 + 十章）。非 Agent 系统：组件红线表按**服务/模块**分类（是否含 LLM、是否持久、是否自主决策、是否写数据）。  
> **端到端权威时序以 §3.15 为准**；各节措辞与之冲突时改各节。

---

## 目录

1. [架构总览](#1-架构总览)
2. [技术栈选型](#2-技术栈选型)
3. [核心架构](#3-核心架构)
4. [扩展体系](#4-扩展体系)
5. [数据访问层](#5-数据访问层)
6. [网关与集成](#6-网关与集成)
7. [安全与权限](#7-安全与权限)
8. [开发模式](#8-开发模式)
9. [关键技术决策记录](#9-关键技术决策记录)
10. [附录](#10-附录)

---

## 1. 架构总览

### 1.1 设计原则（12 条 · 机制化）

| # | 原则 | 机制化解释（可检查） |
|---|------|----------------------|
| P1 | **只荐不卖** | 系统不提供订单、支付、账号履约；「去购票」仅外链/Toast stub；任何 PR 引入收款能力直接拒审（§7.5） |
| P2 | **程序算计划** | 方案枚举、MCT、打分、三主卡挑选只走 `pipeline` + `mct` + `score`；`service_ref` / `price_ref_cny` **原样来自适配器**，引擎禁止改写或臆造 |
| P3 | **LLM 禁编班次票价** | 模型若接入，仅允许口语偏好解析或「为什么」文案润色；禁止输出/覆盖 `Leg.service_ref`、票价字段；默认关闭 LLM（§3.1 C7） |
| P4 | **窄腰宽边** | 引擎只消费统一 `Leg[]` / `TransferPlay`；火车/航班/未来真源一律经 Adapter 归一；前端只消费 `PlansSearchResponse` 形（§5） |
| P5 | **Mock 可演示优先** | MVP 对外演示允许纯 `source:"mock"`（已拍板）；UI 必须展示「价格、时刻均为参考，以购票平台为准」 |
| P6 | **失败可解释** | 无腿 / MCT 不可行 / 校验失败 → 明确 `ok:false` + `reason`/`error.code` + 前端空态文案；**禁止静默编造方案** |
| P7 | **换乘阈值硬约束** | 同站 MCT 默认 60min、同城 180min；缓冲低于 `TransferPlay.min_buffer_hours` **不挂**「怎么玩」 |
| P8 | **三主卡契约** | 有方案时产出最省钱 / 最快 / 最综合（`cheap`/`fast`/`balanced`）；更多方案进 `more`；2 次以上中转默认不进三主卡（MVP） |
| P9 | **密钥零入库** | 密钥仅环境变量 / 密钥托管；`.gitignore` 忽略 env；CR 拒绝含 Key 的 commit；公开仓不写供应商商务细节 |
| P10 | **禁止违规爬取** | 不得模拟登录、绕验证码、批量抓购票页；真源仅合规 API / 已签约渠道（§5.6） |
| P11 | **小程序正式前端** | 微信小程序为正式交付前端；`index.html` Vue CDN 原型**非生产、冻结为演示对照**，不再作为生产演进主线 |
| P12 | **一处定义处处引用** | `Leg`/`TransferPlay` 以 Schema + `data-contract-mock.md` 为准；枢纽以 `hubs-mvp.md` 12 城为准；API 以 `api-plans-contract.md` 为准；术语见 §10.4 |

**原则 ↔ 机制映射（无孤儿）**

| 原则 | 主要机制章节 |
|------|--------------|
| P1 | §3.1、§7.1、§7.5、§8.13 不实现清单 |
| P2 | §3.3–§3.8、§3.15 |
| P3 | §1.2、§3.1 C7、§4.1、§9 ADR-03 |
| P4 | §1.3、§5.1–§5.3 |
| P5 | §5.4、§8.13 Phase-0 |
| P6 | §3.12、§6.3、空态规范 |
| P7 | §3.5 MCT、§3.7 POI 挂载 |
| P8 | §3.6 score、§3.8 Plan |
| P9–P10 | §5.6、§7.1 L1–L2、§7.5 |
| P11 | §2、§6.1、§8.13、§9 ADR-01 |
| P12 | §10、交叉引用全文 |

### 1.2 组件类型红线表

> 分类口径在本表、§3.1 设计约束与 §10.4 术语表三处保持一致。

| 类型 | 有无 LLM | 是否持久 | 是否自主决策 | 是否允许写数据 | 典型示例 |
|------|----------|----------|--------------|----------------|----------|
| **FE-Mini** 小程序前端 | 否 | 否（仅本地 globalData / 静态 JSON） | 否 | 否（只读 mock / 未来只读 API） | `miniprogram/pages/*` |
| **FE-Proto** Web 原型 | 否 | 否 | 否 | 否 | `index.html` + `js/app.js`（**冻结**） |
| **ENG-Pipe** 规则流水线 | 否 | 否 | 否（确定性枚举） | 否 | `engine/pipeline.js` |
| **ENG-Rule** 规则模块 | 否 | 否 | 否 | 否 | `mct.js`、`score.js` |
| **ADP-Mock** Mock 适配器 | 否 | 进程内 TTL 缓存 | 否 | 仅写内存缓存条目 | `train.mock.js`、`flight.mock.js` |
| **ADP-Vendor** 合规源适配器（后续） | 否 | 缓存层 | 否 | 仅写缓存；凭据不落仓 | `train.<vendor>.js`（规划） |
| **DATA-Static** 静态内容 | 否 | 文件 | 否 | 人工 PR 改文件 | `data/mock/legs-*.json`、`data/hub-pois.json` |
| **TPL-Copy** 文案模板 | 否（润色可选 LLM） | 否 | 否 | 否 | `why_facts` → 模板字符串 |
| **LLM-Optional** 可选讲解员（默认关） | 是 | 否 | 否（不得决策班次） | 否 | 偏好解析 / 润色（未实现） |
| **GW-API** HTTP 网关（后续） | 否 | 可选访问日志 | 否 | 审计日志可写 | `POST /v1/plans/search`（契约已定，服务待接） |

**判定准则（新需求先归类）**

| 能力特征 | 归入 |
|----------|------|
| 需要自然语言理解但不影响班次/票价 | LLM-Optional（且强制出口校验：不得含 `service_ref`/价格覆写） |
| 需要确定性组合 / 打分 / MCT | ENG-Rule / ENG-Pipe |
| 需要取时刻价 | ADP-*（禁止 FE 直连供应商） |
| 需要展示与交互 | FE-Mini（生产）/ FE-Proto（仅演示） |
| 需要持久用户账号/订单 | **拒收**（违反 P1） |
| 归类失败 | 视为设计漏洞，不得实现 |

### 1.3 总体架构图

```text
┌─────────────────────────────────────────────────────────────────┐
│  用户（微信）                                                      │
│    └─ FE-Mini 小程序（正式）  query → results → detail → about   │
│         │ 读本地 plans JSON（MVP） / 未来 POST /v1/plans/search   │
│  ─────────────────────────────────────────────────────────────  │
│  FE-Proto index.html（Vue3+Vant4 CDN）—— 非生产冻结，仅对照演示     │
└────────────────────────────┬────────────────────────────────────┘
                             │ PlansSearchResponse 形
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  GW-API（后续）POST /v1/plans/search                               │
│    └─ ENG-Pipe  pipeline.searchPlans                              │
│          ├─ ADP  searchLegs = train.mock ∥ flight.mock            │
│          ├─ HUBS_MVP / vias 定链（自动 1 转 / 指定途经）           │
│          ├─ ENG-Rule mct.checkConnection                          │
│          ├─ DATA-Static hub-pois → TransferPlay（阈值挂载）       │
│          └─ ENG-Rule score.pickMainCards → main + more            │
└────────────────────────────┬────────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
   data/mock/legs-*.json  data/hub-pois.json  合规 vendor（后续）
   source:mock + as_of     12 城 POI           密钥仅 env
```

**系统边界一句话**：输入 OD（可选日期、有序途经）→ 程序产出可解释的换乘组合推荐 → 外链购票；不卖票、不编班次、不爬票务页。

---

## 2. 技术栈选型

### 2.1 层级 × 技术 × 版本 × 理由

| 层级 | 技术 | 版本 / 形态 | 选型理由 |
|------|------|-------------|----------|
| 正式前端 | 微信小程序 + **Vant Weapp** | `@vant/weapp` ^1.11.7 | 拍板禁止自研设计系统；与产品移动端心智一致 |
| Web 原型（冻结） | Vue 3 + Vant 4（CDN） | CDN，无构建 | 快速对外演示；**非生产** |
| 规则引擎 | Node.js CommonJS | 仓库内 `engine/*.js` | 零新依赖可冒烟；与小程序工具链同 JS 生态 |
| 历史/对照脚本 | Python 3 | `search.py` / `score.py` / `export_fe.py` | 早期导出；**运行时主链以 Node `pipeline.js` 为准** |
| 数据契约 | JSON Schema draft 2020-12 | `docs/schemas/*.json` | Leg / TransferPlay 字段级校验锚点 |
| Mock 数据 | 静态 JSON | `data/mock/`、`data/hub-pois.json` | 已拍板纯 mock 可演示 |
| 缓存（MVP） | 进程内 Map + TTL | 默认 **15 min**（`DEFAULT_TTL_MS`） | 拍板：不上 Redis，待 QPS/多实例再上 |
| 地图/接驳（后续） | 官方/合规 SDK | — | 不自研路网 |
| 密钥 | 环境变量 / 云密钥管理 | — | 密钥不进仓 |

### 2.2 为什么选「小程序 + 规则引擎」，不自研设计系统 / 不 LLM 出方案

| 问题 | 选择 | 不选的理由 |
|------|------|------------|
| UI 组件 | Vant Weapp / Vant 4 | 自研设计系统成本高、拍板禁止造轮子 |
| 出计划 | 确定性 pipeline | LLM 编车次票价不可审计、易幻觉、违反产品原则 |
| 前端主线 | 微信小程序 | 目标用户在微信内；Web 原型仅保真演示 |
| 数据 | Adapter + Schema | 引擎与供应商解耦；mock/真源字段同构可切换 |

### 2.3 许可与迁移成本

- Vant / Vue：成熟开源，CDN/npm 引入成本低。  
- 引擎无外部运行时依赖（除 Node 标准库 + 本地 JSON），迁移到云函数/单机 HTTP 服务成本低。  
- 真源切换只需新增 `train.<vendor>.js` 并在 `adapters/index.js` 注册，**不改** `searchPlans` 消费者契约（§4、§5）。

---

## 3. 核心架构

### 3.1 设计约束（与红线表口径一致）

| ID | 约束 |
|----|------|
| C1 | 不卖票、不做支付/订单/抢票/账号履约 |
| C2 | 出计划仅 ENG-Pipe / ENG-Rule；禁止 FE 本地「编」车次票价 |
| C3 | Adapter 输出必须符合 `leg.schema.json`；缺字段不得用模型补全班次价 |
| C4 | MCT：同站默认 60min、同城默认 180min；不可衔接组合丢弃 |
| C5 | 自动模式 MVP：**至多 1 次中转**进候选（指定途经按段数，途经 ≤ 3） |
| C6 | 「怎么玩」仅当 `buffer_hours >= poi.min_buffer_hours`；12 城以外可只显示接驳 |
| C7 | LLM 默认关闭；开启时不得写入 `service_ref` / `price_*` |
| C8 | 降级路径：真源 → 未过期缓存 → mock → 可解释错误；**禁止**降级时编造 |
| C9 | `index.html` 原型冻结：缺陷以小程序为准修复；原型仅安全/文案同步级改动 |
| C10 | 价格时刻展示带「参考」；无实时余票承诺 |

### 3.2 组件形态分类

| 形态 | 定义 | 适用 | 不适用 |
|------|------|------|--------|
| 流水线编排器 | 单入口 `searchPlans` 串起取腿→定链→MCT→POI→打分 | 查询主路径 | 承载 UI、密钥、爬虫 |
| 纯函数规则 | 无 I/O（或只读入参）的 MCT/打分 | 可单测、可重放 | 持有 DB 凭据 |
| 适配器 | I/O 边界，归一为 `Leg[]` | mock / 合规 API | 业务打分、文案产品逻辑 |
| 静态内容表 | 人工维护 JSON | 枢纽 POI、样例腿 | 运行时由模型生成 |
| 前端壳 | 页面状态机 + 适配层 | 展示三主卡/空态 | 枚举换乘组合 |

**新建模块自证准则（满足 ≥3 条才建）**

| # | 准则 | 说明 |
|---|------|------|
| 1 | 有独立失败半径 | 故障不应拖垮整条 pipeline |
| 2 | 有独立契约/Schema | 可单独版本化 |
| 3 | 有独立测试面 | 可单测或冒烟 CLI |
| 4 | 跨 ≥2 消费者复用 | 避免一次性脚本升格为模块 |
| 5 | 红线表可归类 | 归不进则不建 |
| 6 | 无密钥泄漏面扩大 | 不新增明文凭据路径 |
| 7 | 不与现有模块职责重叠 | 重叠则扩展而非新建 |

**自证表（当前核心模块）**

| 模块 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 满足数 | 结论 |
|------|---|---|---|---|---|---|---|--------|------|
| `pipeline.js` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 7 | 保留 |
| `mct.js` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 7 | 保留 |
| `score.js` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 7 | 保留 |
| `adapters/index.js` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 7 | 保留 |
| `train.mock.js` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 7 | 保留 |
| `flight.mock.js` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 7 | 保留 |
| 独立「LLM 出方案 Agent」 | ✗ | ✗ | ✗ | ✗ | ✗ | ? | ✗ | 0–1 | **取消/禁止** |
| 独立支付服务 | ✗ | — | — | — | ✗ | ✗ | ✗ | 0 | **取消** |
| 自研设计系统 | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✗ | 1 | **取消** |

**取消 / 不建的组件（负面清单）**

| 曾设想 | 理由 |
|--------|------|
| LLM 生成车次/票价 | 违反 P2/P3；不可重放 |
| 爬虫取 12306/OTA | 违反 P10；法务风险 |
| Redis（MVP） | 拍板单机内存足够演示 |
| 用户账号 / 订单库 | 违反 P1 |
| 昆明进 MVP 玩法表 | 拍板杭州进、昆明出；后续候补 |

### 3.3 角色映射

| 角色 | 职责 | 不负责 |
|------|------|--------|
| 产品 / 负责人 | 拍板枢纽、红线、演示保真度 | 写爬虫、塞密钥进仓 |
| 引擎开发 | pipeline / mct / score / 导出 | 卖票履约 |
| 数据内容 | legs mock、hub-pois、合规源短名单（私有） | 改打分公式而不提 PR |
| 小程序前端 | 四页交互、空态、adapt 层 | 在端上枚举 MCT 组合当权威 |
| 运维（后续） | 密钥托管、网关、观测 | 放宽爬取策略 |

### 3.4 协作机制与 Artifact

| Artifact | 本质 | 生产者 | 消费者 | 治理 |
|----------|------|--------|--------|------|
| `Leg` | 分段时刻价真相 | Adapter | pipeline | Schema 必填；`source`+`as_of` |
| `TransferPlay` | 枢纽玩法 | 人工 JSON | pipeline.pickPlays | 独立表，不嵌 Leg |
| `Plan`（buildPlan） | 候选方案 | pipeline | score / 导出 / 前端 | 含 legs、transfers、play |
| `main` 三槽 | 主卡 | score.pickMainCards | API/前端 | cheapest/fastest/balanced |
| `PlansSearchResponse` | 对外契约 | 网关/导出 | FE | 见 api-plans-contract |
| `why_facts` | 模板结构化字段 | 引擎 | 文案层 | 禁止含臆造车次 |

**聚合边界**：一次 `searchPlans` 调用内完成枚举与打分；前端不得二次「发明」腿。小程序 MVP 读预导出 JSON，等价于离线固化的一次 pipeline 结果。

### 3.5 MCT（最小换乘时间）

实现：`engine/mct.js`。

| 项 | 默认值 | 说明 |
|----|--------|------|
| `same_station` | **60** 分钟 | 到站名 === 下一腿出发站名 |
| `same_city` | **180** 分钟 | 同城不同站 |
| 无法分类 | `ok:false` | 跨城未衔接，丢弃 |
| 缓冲计算 | `floor((dep_at - arr_at) / 60000)` | ISO 带时区 |

失败路径：整链任一段 `!ok` → 不进入 `plans`（§3.12）。

### 3.6 打分与三主卡

实现：`engine/score.js`。**分越低越好**。

| 槽位 | 函数 | 主因子 | 次因子 |
|------|------|--------|--------|
| 最省钱 `cheapest` | `scoreCheapest` | `price_ref_cny` | `duration/1e6` |
| 最快 `fastest` | `scoreFastest` | 门到门 `duration_min` | `price/1e6` |
| 最综合 `balanced` | `scoreBalanced` | 价+时+换乘+单段久坐 | 航班惩罚、comfort、可玩窗口加分 |

综合默认权重（`DEFAULT_BALANCED_WEIGHTS`）：

| 键 | 默认 |
|----|------|
| `price` | `1/650` |
| `duration` | `1/2000` |
| `transfers` | `0.35` |
| `maxLegSitMin` | `1/3000` |

> 综合偏铁路卧铺 + 可玩窗口；空铁更易进「最快」。产品文案层勿暴露「加权系数」工程句（见空态与文案规范）。

### 3.7 定链与 POI 挂载

| 模式 | 定链规则 | 实现 |
|------|----------|------|
| 自动 `auto` | 直达 + 经 `HUBS_MVP`（12 城）插 **1** 个中转；可选 `extraHubs` 短途样例 | `searchPlans` else 分支 |
| 指定路径 `user` | `from + vias(≤3) + to` 分段；不再自动换枢纽 | `searchChainCities` |

POI：`data/hub-pois.json`；`pickPlays(hub, bufferMin)` 过滤 `hours >= min_buffer_hours`，按阈值降序取最多 **3** 条。

**12 城（锁定）**：北京、上海、广州、深圳、成都、重庆、武汉、杭州、郑州、西安、兰州、**西宁**（进藏样例不可删）。

### 3.8 核心数据结构

#### 3.8.1 `Leg`（字段级 · 与 Schema 一致）

```json
{
  "id": "leg-xz-xn-z165",
  "mode": "train",
  "from_city": "徐州",
  "to_city": "西宁",
  "from_station": "徐州站",
  "to_station": "西宁站",
  "dep_at": "2026-10-01T19:30:00+08:00",
  "arr_at": "2026-10-02T14:40:00+08:00",
  "duration_min": 1150,
  "price_ref_cny": 320,
  "price_min_cny": 280,
  "price_max_cny": 560,
  "seat_hint": "硬卧",
  "service_ref": "Z165",
  "comfort": "hard_sleeper",
  "source": "mock",
  "as_of": "2026-09-22T12:00:00+08:00"
}
```

#### 3.8.2 方案状态枚举（查询结果）

| 状态 | 含义 | 安全默认 |
|------|------|----------|
| `OK` | `ok:true` 且至少一张主卡 | 展示三主卡 |
| `INVALID_REQUEST` | OD 缺失、途经超限等 | 留在查询页 Toast |
| `NO_LEGS` | 源无可用腿 | 空态文案 |
| `NO_FEASIBLE` | 有腿但 MCT 下无组合 | 空态文案 |
| `INTERNAL` / 加载失败 | 网关或读盘失败 | 失败态「数据暂不可用」 |
| `UNKNOWN_PLAN` | 详情 id 无效 | Toast 回结果/查询 |

缺失策略：无 `comfort` → 打分按 `unknown=0.4`；无 POI → `play=null`，只提示接驳；**缺失价格不得用 LLM 填**。

#### 3.8.3 版本治理

| 要素 | 位置 |
|------|------|
| 数据契约版本 | Schema `$id` + `data-contract-mock.md` 定稿声明 |
| 引擎行为 | git commit；导出 JSON 的 `meta.as_of` |
| API | `/v1/plans/search`；破坏性变更升 `/v2` |
| 小程序 | `miniprogram/package.json` version（当前 0.2.0） |

### 3.9 流水线步骤（实现约束）

1. 校验 OD / vias≤3  
2. `searchLegs`（train∥flight）取直达与分段腿  
3. 定链（自动枢纽或用户途经）  
4. 组合枚举，cap 默认 **32** 条链  
5. MCT 过滤  
6. `buildPlan` + `attachPlays`  
7. `pickMainCards` → `main`；全量进 `more`  
8. 填充展示字段 / `why_facts`（导出或网关层）；`service_ref` 透传  

### 3.10 路由与通讯

| 通道 | 现状 | 禁止 |
|------|------|------|
| 小程序本地 `require` JSON | MVP 正式演示路径 | 在端上写死假车次当「引擎」 |
| `node engine/search-pipeline-demo.js` | 冒烟 | 用 LLM 替代 pipeline |
| 未来 `POST /v1/plans/search` | 契约已定 | FE 直连供应商 API 带 Key |

### 3.11 反馈闭环（产品级）

| 信号 | 用途 | MVP |
|------|------|-----|
| 外链点击 / Toast「去购票」 | 成功标准代理 | 仅 stub，可后续埋点 |
| 空态「看看示例」 | 覆盖率不足时的逃生 | 已有 |
| 人工对答案（徐拉等热门线） | 引擎回归 | 建议保留样例导出 |

### 3.12 失败与降级表

| 场景 | 策略 | 禁止 |
|------|------|------|
| 校验失败 | Toast，不跳转 | 跳进空结果假装成功 |
| Adapter `LEGS_NOT_FOUND` | 当空数组；可能 `NO_FEASIBLE` | 编造 Leg |
| MCT 失败 | 丢弃该链 | 放宽到负缓冲「碰运气」 |
| 真源超时（后续） | L2/L3 缓存 → mock → 错误 | 模型补班次 |
| 部分模式无航班 | 只出铁路方案 | 硬凑空铁 |
| 详情 id 丢失 | Toast 回退 | 展示空白详情 |

### 3.13 可重放机制

**Reproducible Result ≈**  
`legs 快照(source,as_of)` + `hub-pois 版本` + `HUBS_MVP` + `MCT 默认值` + `score 权重` + `pipeline 代码 commit` + `请求(from,to,date,vias)`。

导出命令（样例）：

```bash
node engine/export-plans-with-flight.js
node engine/search-pipeline-demo.js
```

### 3.14 端到端协同示例

**自动模式 · 徐州→拉萨**

1. 取直达腿 + 经西宁/西安等枢纽两段腿（含可选航班）  
2. MCT 过滤  
3. 西宁缓冲够 → 挂塔尔寺等 POI  
4. 三主卡：偏硬座省钱 / 空铁最快 / 西宁进藏综合  

**指定路径 · vias=["西宁"]**

1. 只枚举 徐州→西宁→拉萨  
2. `path_note`：「因你指定经 西宁」  
3. 三主卡为同路径下不同班次组合  

### 3.15 端到端权威时序图

> **本图统一各处措辞。** 与本图冲突的描述以本图为准并回改文档。

```text
用户                    FE-Mini                 ENG-Pipe / 本地JSON           ADP-Mock
 │                        │                            │                        │
 │ 填 OD/日期/途经         │                            │                        │
 │──开始邪修─────────────►│                            │                        │
 │                        │ 校验（空/同城/途经≤3）        │                        │
 │                        │──失败 Toast（留查询页）──────│                        │
 │                        │                            │                        │
 │                        │ 加载中 ~700ms               │                        │
 │                        │──(MVP) require plans-*.json─►│                        │
 │                        │  或 (后续) POST /v1/plans/search                      │
 │                        │                            │ searchPlans             │
 │                        │                            │──searchLegs(from,to)───►│
 │                        │                            │◄── Leg[] source=mock ───│
 │                        │                            │ 定链 HUBS/vias           │
 │                        │                            │──searchLegs 分段────────►│
 │                        │                            │  MCT checkConnection     │
 │                        │                            │  pickPlays(hub-pois)     │
 │                        │                            │  pickMainCards           │
 │                        │◄── main + more / 或 ok:false─│                        │
 │◄─三主卡 / 空态/失败─────│                            │                        │
 │ 点主卡                  │                            │                        │
 │──详情（怎么去/为什么/怎么玩）►│                      │                        │
 │ 点「去购票」             │ Toast：不卖票，去12306/OTA   │                        │
 │◄───────────────────────│  （无支付、无订单）          │                        │
```

---

## 4. 扩展体系

### 4.1 四类扩展边界

| 类型 | 本质一句话 | 允许条件 | 确定性 | 谁执行 |
|------|------------|----------|--------|--------|
| **Skill（怎么做）** | 导出/适配/运营手册级步骤 | 不改红线 | 流程可重复 | 人 + 脚本 |
| **Policy（何时允许）** | 演示开关、path_mode、缓存 TTL | 产品拍板 | 配置常量 | 配置 |
| **Rule（确定性判断）** | MCT、打分、POI 阈值 | 单测覆盖 | 纯函数 | ENG-Rule |
| **Tool（真正执行）** | `searchLegs`、读 JSON、未来 HTTP 真源 | 权限与密钥边界内 | I/O | ADP / GW |

横切约束：任何扩展不得让 LLM 成为 Rule/Tool 的班次价来源。

### 4.2 Tool 清单

| 名称 | 输入 | 输出 | 权限级别 | 幂等 | 内部组件 |
|------|------|------|----------|------|----------|
| `adapters.searchLegs` | `{from,to,date?,after_at?}` | `Leg[]` | L0 读公开/mock | 是（同参同快照） | train+flight mock |
| `mct.checkConnection` | 两段 Leg | `{ok,kind,buffer_min,required_min}` | L0 | 是 | mct.js |
| `score.pickMainCards` | `Plan[]` | `{cheapest,fastest,balanced}` | L0 | 是 | score.js |
| `pipeline.searchPlans` | `{from,to,date?,vias?,extraHubs?}` | `{ok,main,more,meta}` | L0 | 是（同数据版本） | 编排以上 |
| `export-plans-*.js` | CLI OD 选择 | 写 `data/plans-*.json` | L1 写仓库数据文件 | 否（写文件） | pipeline+模板 |
| 未来 `train.<vendor>.searchLegs` | 同契约 | `Leg[]` | L2 需密钥 | 视上游 | vendor SDK |
| 未来「去购票」深链 | 站名+日期 | 外跳 | L0 无本站收款 | 是 | 仅 URL 模板 |

**权限级别枚举**

| 级别 | 含义 |
|------|------|
| L0 | 只读本地或公开契约，无密钥 |
| L1 | 可写仓库内数据文件（经 PR） |
| L2 | 持密钥调外部 API（密钥不进仓） |
| L3 | 写用户隐私 / 支付 — **本产品不开放** |

### 4.3 扩展类型判断矩阵

| 能力特征 | 优先实现为 |
|----------|------------|
| 新合规数据源 | ADP-Vendor Tool + Policy(TTL) |
| 新打分维度 | Rule（改 score + 单测） |
| 新枢纽城 POI | DATA-Static + hubs 文档 |
| 口语「便宜就行」 | 可选 LLM-Optional → 映射到已有权重，**不**新编腿 |
| 卖票/会员支付 | **拒绝** |

### 4.4 元数据示例（Vendor 适配器 · 目标）

```yaml
# engine/adapters/train.example.meta.yaml（示意，非仓库必有文件）
name: train.example
mode: train
contract: docs/schemas/leg.schema.json
auth:
  type: env
  vars: ["TRAIN_API_KEY"]   # 值禁止入库
cache_ttl_ms: 900000
scrape: false
```

治理：新增 vendor 必须 PR 审核「无爬取、无密钥、输出 Schema 校验」；灰度先暗流量对比 mock；回滚=适配器开关切回 mock。

---

## 5. 数据访问层

### 5.1 设计动机（挑战）

| 挑战 | 应对 |
|------|------|
| 票源合规与反爬 | 只走授权 API；禁爬 |
| mock 与真源切换 | 同一 `Leg` Schema |
| 价格易变 | TTL + `as_of` + 文案「参考」 |
| 引擎被供应商 SDK 污染 | 双路径：内部 Repository 形函数，外部 Adapter |
| 密钥泄漏 | 集中 env；CR 阻断 |

### 5.2 双路径

```text
ENG-Pipe
  └─ searchLegs()          ← 内部稳定契约（Repository 角色）
        ├─ train.mock / flight.mock     （当前）
        └─ train.<vendor> / flight.<vendor>（后续）
              └─ 合规 HTTP；密钥仅 process.env
```

协议定位一句话：**Adapter 是唯一允许接触外部票务数据的边界；引擎永不持有供应商 SDK 类型。**

### 5.3 职责分离

| 组件 | 职责 | 不负责 | 经协议层 |
|------|------|--------|----------|
| pipeline | 定链、组合、组装 Plan | 调供应商 SDK | 是（只调 searchLegs） |
| score/mct | 纯计算 | I/O | 否 |
| adapters | 取腿、缓存、日期平移 | 打分、POI 文案 | 自身即边界 |
| hub-pois | 玩法内容 | 时刻价 | 文件只读 |
| FE | 展示 | 生成 service_ref | 否 |

### 5.4 降级阶梯（与 data-sources 一致）

```text
真源 API（成功且缓存未过期）
  → L2/L3 未过期缓存
    → 预置 mock（source:"mock"）
      → 可解释错误（NO_LEGS / NO_FEASIBLE）
```

MVP 现状：直接 mock + 内存 TTL 15min；真源未接。

### 5.5 Adapter 原语

| 原语 | 用途 | 返回 |
|------|------|------|
| `searchLegs(query)` | 按 OD 取腿 | `Leg[]` |
| `configure({ttlMs,legsDir})` | 测试注入 | void |
| `clearCache()` | 测试/运维 | void |

受控查询：mock 为文件过滤；未来 SQL/开放数据必须白名单模板，**禁止**组件持有自由 SQL 凭据拼接。

### 5.6 引擎适配规则与引入时机

| 规则 | 内容 |
|------|------|
| 火车优先 | MVP 主路径铁路；航班服务「最快」与空铁 |
| 字段同构 | vendor 输出必须通过 leg.schema |
| 部分成功 | 无航班仍可出铁路方案 |

**引入 Redis / 多实例缓存的量化信号（建议）**

| 信号 | 阈值 |
|------|------|
| 单机 p95 查询（含取腿） | > 3s 且 CPU 闲置于重复读盘 |
| 实例数 | ≥ 2 且缓存命中成为瓶颈 |
| QPS | 持续 > 20（量级示意，上线前再测） |

真源引入：商务短名单私有；公开文档只写「合规 API」。

### 5.7 快照与血缘

- 每条 Leg：`source` + `as_of`  
- 导出 plans：文件路径 + git blame  
- 查询 meta：回显 request、`data_source`、`disclaimer`  

### 5.8 权限模型（精简）

本产品无多租户 PHI；权限维度主要为：

| 维度 | 控制 |
|------|------|
| 代码仓库 | 无密钥；CR |
| 运行时 | vendor Key 仅服务器 env |
| 前端 | 永不嵌入 Key；深链不含凭证 |

预留：capability token 仅在未来 B 端 API 时设计；MVP 不实现。

---

## 6. 网关与集成

### 6.1 集成模式

| 客户端 | 模式 | 说明 |
|--------|------|------|
| 微信小程序 | 本地 JSON → 未来 HTTPS API | 正式前端 |
| Web 原型 | 本地 JSON / 可选 pipeline 映射 | 冻结 |
| CLI | `node engine/*-demo.js` | 研发冒烟 |

优势：契约先行（api-plans-contract）；前端 `adapt-plans` / `normalizePipelineResponse` 薄映射 snake_case↔UI。

### 6.2 后端路由骨架（目标）

```js
// 示意：后续服务入口，非当前仓库必有文件
app.post('/v1/plans/search', async (req, res) => {
  const body = req.body; // PlansSearchRequest
  // 1. 校验 INVALID_REQUEST
  // 2. const out = await searchPlans({ from: body.from_city, to: body.to_city, ...})
  // 3. 映射 main 对象槽 → main[]（type=cheap|fast|balanced）
  // 4. res.json(PlansSearchResponse)
});
```

### 6.3 事件流（前端渲染）

| 事件/状态 | 前端渲染 | 说明 |
|-----------|----------|------|
| 校验失败 | Toast | 留查询页 |
| loading | 「正在邪修组合…」 | ~700ms 演示 |
| `ok=true` | 三主卡 + 更多 | 主路径 |
| 无方案 | 空态 + 示例线 | 文案规范 |
| 加载失败 | 失败态 + 再试/改条件 | 不甩锅用户 |
| 去购票 | Toast stub | 不卖票 |
| 关于 | 免责声明 | 参考价说明 |

### 6.4 小程序页面映射

| 页面 | 路径 | 职责 |
|------|------|------|
| 查询 | `pages/query` | OD、途经≤3、样例、演示开关 |
| 结果 | `pages/results` | 三主卡、空/失败 |
| 详情 | `pages/detail` | 怎么去/为什么/怎么玩/购票 stub |
| 关于 | `pages/about` | 只荐不卖、免责 |

---

## 7. 安全与权限

### 7.1 分层安全模型（4 层）

**L1 供应链与仓库**

- 密钥不进仓；env 不提交  
- 禁止依赖来路不明的票务爬虫包  
- CR 检查：无 Key、无爬取代码、无支付 SDK  

**L2 运行时数据**

- Adapter 边界；前端无供应商凭据  
- 缓存键不含密钥；响应可缓存，凭证不可明文落盘进仓  
- mock `source` 明示  

**L3 产品合规表达**

- 全程「参考价/时刻」；不承诺有票  
- 外链购票；本站不收款  
- 免责声明页可触达  

**L4 可选模型面（默认关）**

- 输出过滤：拒绝含车次号/价格覆写的补丁  
- 不进 Leg 写路径  

### 7.2 高风险操作审批链

本系统高风险不在「写病历」，而在「接真源 / 引入爬取 / 接支付」：

```text
提案（PR 说明合规依据）
  → 负责人预览影响（数据用途、是否进仓、是否对用户收款）
    → 批准（仅合规 API / 仍禁止爬取与卖票）
      → 合并 + 密钥注入托管
        → 审计（commit / 变更单）
          → 可撤销：适配器开关切回 mock（窗口：发现违规立即）
```

### 7.3 权限矩阵

| 角色 × 操作 | 读 mock | 改打分 | 加 vendor | 加支付 | 导出 plans |
|-------------|--------|--------|-----------|--------|------------|
| 前端 | ✓ | ✗ | ✗ | ✗ | ✗ |
| 引擎 | ✓ | ✓（PR） | ✓（PR） | ✗ | ✓ |
| 负责人 | ✓ | 审批 | 审批 | **永久否** | 审批发布 |
| 匿名用户 | 仅小程序展示 | ✗ | ✗ | ✗ | ✗ |

### 7.4 审计与合规

| 项 | MVP | 后续 |
|----|-----|------|
| 变更审计 | git history | 正式变更单 |
| 数据来源披露 | about 页 + disclaimer | 同源 |
| WORM/外部锚定 | 不需要（无资金流水） | 若 B 端 API 再评估 |

### 7.5 上线前置阻断项

| 阻断项 | 判定内容 | 未完成后果 | 责任归口 |
|--------|----------|------------|----------|
| 卖票/支付代码 | 仓库无订单支付闭环 | **禁止上架** | 负责人 |
| 密钥入库 | git 历史与树无 Key | 禁止发布；轮换密钥 | 全员 + CR |
| 违规爬取 | 无模拟登录/抓购票页 | 禁止发布 | 引擎/数据 |
| 参考价文案 | 主路径可见「参考」「以购票平台为准」 | 禁止对外演示宣称实时票 | 前端/产品 |
| 小程序隐私合规 | 按微信平台要求填写（无未声明采集） | 无法过审 | 前端 |
| LLM 班次隔离 | 若开启润色，有输出护栏 | 关闭 LLM 功能 | 引擎 |
| 原型误当生产 | README 标明小程序正式、index 冻结 | 发布说明纠正 | 发布 |

---

## 8. 开发模式

### 8.1 总体原则

- **双轨**：轨 A 引擎/数据正确性；轨 B 小程序可演示体验。  
- 前置依赖：字段契约定稿 → mock 腿/POI → pipeline 冒烟 → 导出 plans → 小程序挂样例。  
- 禁止造轮子（拍板）；优先开源组件。

### 8.2 两条轨范围

| 轨 | 范围 | 完成定义 |
|----|------|----------|
| A 引擎 | adapters、mct、score、pipeline、导出 | `search-pipeline-demo` 绿；徐拉等 OD 对得上三主卡直觉 |
| B 前端 | miniprogram 四页 + 文案规范 | DEMO.md 走查 1 分钟可过 |

### 8.3 扩展工作流（8 步）

以「新增合规火车源」为例：

1. 判断需要 ADP-Vendor（矩阵 §4.3）  
2. 编写 `train.<vendor>.js` 输出 Leg  
3. Schema 校验  
4. 单测 / 与 mock 对比抽样 OD  
5. PR（无密钥、无爬取）  
6. 负责人审核  
7. 灰度：开关默认 mock，暗流量对比  
8. 监控错误率；异常回滚 mock  

### 8.4 环境与发布

| 环境 | 用途 |
|------|------|
| 本地 | 开发者工具 + `node` 冒烟 |
| 体验版小程序 | stakeholder 演示 |
| 正式版 | 过审后；仍只荐不卖 |

配置约束：README 只列环境变量**名**；`.gitignore` 忽略 env；`project.private.config.json` 不提交敏感项（已存在于仓则避免写入密钥）。

### 8.5 测试与评估

| 类型 | 内容 |
|------|------|
| 单元 | mct 边界、score 选主卡、adapter 过滤 mode |
| 冒烟 | `search-pipeline-demo.js`、各 adapter CLI |
| 契约 | plans 导出字段 ⊆ api-plans-contract |
| 体验 | DEMO.md 路径 A/B/C；空态走查 |
| 领域评估集 | 热门 OD（徐拉、沪蓉、京广、成渝）人工对「为什么」直觉 |

评估集来源：`data/mock/legs-*.json` + 导出 plans；禁止用模型生成假腿充评估集。

### 8.6 部署架构

**Dev**

```text
开发者机
  ├─ 微信开发者工具 → miniprogram/
  ├─ node engine/*-demo.js
  └─ 可选 python3 -m http.server（仅看冻结原型）
```

**生产（目标）**

```text
微信小程序（HTTPS）
  └─ 云托管 / 单机 Node  （POST /v1/plans/search）
        └─ adapters（mock 或 vendor）+ 内存/Redis 缓存
```

配置：无 Key 默认 mock；有 Key 才启 vendor。

### 8.7 迭代模式

小步：改契约 → 改引擎 → 重导 JSON → 同步 `miniprogram/data` → 走查。每阶段 commit；push 前 `git pull --rebase`。

### 8.8 分阶段实施

#### Phase-0 可演示 MVP（当前 · 2026-09）

| 维 | 内容 |
|----|------|
| 目标 | 小程序 + mock 三主卡对外演示；引擎 pipeline 可冒烟 |
| 保留 | 12 城 POI、四线样例 OD、Vant Weapp、冻结 Web 原型 |
| 前置必做 | 契约、空态文案、免责声明 |
| **不实现** | 真源、Redis、登录、支付、LLM、全国站码、2 次以上中转进主卡 |
| 新增 | 本架构文档；按需补 API 服务壳 |
| 关闭 | 爬取实验、自研 UI 库、昆明强行入 MVP 玩法 |

#### Phase-1 真源试点

| 维 | 内容 |
|----|------|
| 目标 | 1 家合规火车源；航班仍可 mock |
| 保留 | Schema、降级到 mock |
| 前置 | 商务合同、密钥托管、阻断项清单打勾 |
| 不实现 | 卖票、全量地级市玩法 |
| 新增 | `train.<vendor>.js`、基础观测 |
| 关闭 | 任何 HTML 抓取 PoC |

#### Phase-2 可用全国版（方向）

| 维 | 内容 |
|----|------|
| 目标 | 地级市车站/机场覆盖 + 稳定 1 转 |
| 保留 | 只荐不卖；程序算计划 |
| 前置 | 缓存策略升级信号触发 Redis |
| 不实现 | 国际线、复杂多日编辑器、UGC |
| 新增 | 地图接驳合规 SDK、评估集扩容 |
| 关闭 | 以 LLM 替代 MCT/score |

**能力 × 阶段对照**

| 能力 | P0 | P1 | P2 |
|------|----|----|----|
| 小程序三主卡演示 | ✓ | ✓ | ✓ |
| pipeline 冒烟 | ✓ | ✓ | ✓ |
| 合规火车真源 | ✗ | ✓ | ✓ |
| 合规航班真源 | ✗ | 可选 | ✓ |
| Redis | ✗ | 按信号 | 预期 |
| LLM 润色 | ✗ | 可选关 | 可选 |
| 支付/卖票 | ✗ | ✗ | ✗ |

**迁移原则**：先跑通主链再扩城；先关闭危险能力（爬取/卖票）再开放受限真源；先建评估 OD 再调权重。

---

## 9. 关键技术决策记录

| ID | 决策 | 选择 | 理由 | 替代方案（否决） |
|----|------|------|------|------------------|
| ADR-01 | 正式前端 | 微信小程序 | 用户触达与拍板 | 以 Vue CDN 原型当生产 |
| ADR-02 | Web `index.html` | 非生产冻结 | 避免双主线 | 继续双端并行大改 |
| ADR-03 | 出计划方式 | 规则引擎 | 可审计、禁幻觉 | LLM 直接编方案/票价 |
| ADR-04 | UI 组件库 | Vant Weapp / Vant4 | 禁止造轮子 | 自研设计系统 |
| ADR-05 | 引擎主链语言 | Node CJS `pipeline.js` | 与适配器统一、零依赖冒烟 | 仅 Python 运行时 |
| ADR-06 | 数据演示策略 | 纯 mock 可演示（已拍板） | 不挡产品 | 无真源不上演示 |
| ADR-07 | 火车 vs 航班 | 火车优先，航班第二 | 空铁服务最快卡 | 一上来全量比价引擎 |
| ADR-08 | 缓存 | 单机内存 TTL 15min | MVP 够用 | 过早上 Redis |
| ADR-09 | 枢纽清单 | 12 城含西宁，杭州进昆明出 | 样例与华东覆盖 | 昆明换杭州；删西宁 |
| ADR-10 | POI 存储 | 独立 `hub-pois.json` | 不嵌 Leg | POI 嵌在每条腿 |
| ADR-11 | MCT 默认 | 同站 60 / 同城 180 | 保守可赶车 | 过松导致误荐 |
| ADR-12 | 三主卡算法 | cheap/fast/balanced 纯函数 | 产品三卡契约 | 单一排序 + 人工贴标签 |
| ADR-13 | 途经 | 有序 ≤3；指定则不自动换枢纽 | PRD | 无序途经；指定仍自动改枢纽 |
| ADR-14 | 中转次数 | 主卡默认 ≤1 转 | MVP 复杂度 | 主卡允许多段连环 |
| ADR-15 | 卖票 | 永不做 | 产品红线 | CPS 自营票 |
| ADR-16 | 密钥 | 仅 env/托管 | 合规 | 写进前端或仓 |
| ADR-17 | 取数手段 | 合规 API；禁爬 | 法务 | 模拟登录爬 12306 |
| ADR-18 | API 字段风格 | 引擎 snake_case | 契约统一 | 引擎直接 camelCase |
| ADR-19 | 「怎么玩」出现 | 阈值门控 | 避免硬推 | 有中转就推景点 |
| ADR-20 | 价格展示 | 参考价 + disclaimer | 无余票 | 显示「有票价」 |
| ADR-21 | 适配器合并 | `index.searchLegs` 合并 train+flight | 单一契约 | 流水线分别硬编码两套 |
| ADR-22 | 评估数据 | 真实维护的 mock 腿 | 可重放 | LLM 生成假腿评测 |
| ADR-23 | 变现 | 当前不优先；可外链 CPS 备忘 | 先做对引擎 | 先接支付再产品 |
| ADR-24 | 文档方法论 | 七律十章架构文 | 可评审可追溯 | 仅 README 碎片 |

---

## 10. 附录

### 10.1 框架关键文件

| 文件 | 用途 |
|------|------|
| `docs/产品需求整理.md` | PRD / 原则 |
| `docs/技术与设计拍板.md` | 负责人拍板 |
| `docs/hubs-mvp.md` | 12 城枢纽 |
| `docs/data-contract-mock.md` | Leg / TransferPlay 字段 |
| `docs/data-sources.md` | 合规源与降级 |
| `docs/api-plans-contract.md` | API 出入参 |
| `docs/空态与文案规范.md` | 空态文案 |
| `docs/小程序-查询与三主卡交互要点.md` | 交互对照 |
| `docs/schemas/leg.schema.json` | Leg Schema |
| `docs/schemas/transfer-play.schema.json` | TransferPlay Schema |
| `engine/pipeline.js` | 权威流水线 |
| `engine/mct.js` / `score.js` | 规则 |
| `engine/adapters/*` | 取腿 |
| `miniprogram/` | 正式前端 |
| `index.html` / `js/app.js` | 冻结原型 |
| `DEMO.md` | 演示路径 |
| `docs/skills/arch-design-doc.md` | 本文方法论 |

### 10.2 协议原语

| 原语 | 用途 | 返回 | 适用 |
|------|------|------|------|
| `searchLegs` | 取分段腿 | `Leg[]` | 一切票源 |
| `checkConnection` | MCT | 结构体 ok/kind/buffer | 一切换乘 |
| `pickMainCards` | 三主卡 | 三槽对象 | 一切候选集 |
| `searchPlans` | 端到端查询 | ok/main/more/meta | 网关/CLI |
| `pickPlays` | POI 过滤 | TransferPlay[] | 缓冲门控 |

### 10.3 目录结构（与仓库一致）

```text
xiexiu-traffic-prototype/
├── README.md
├── DEMO.md
├── index.html                 # Web 原型（冻结）
├── css/ styles.css
├── js/ app.js
├── data/
│   ├── hub-pois.json          # 12 城 TransferPlay
│   ├── plans-*.json           # 前端用导出
│   ├── mock/
│   │   ├── legs-*.json
│   │   └── plans-*.json       # API 形
│   └── README.md
├── docs/
│   ├── 邪修交通-系统架构设计_v1.0.md  # 本文
│   ├── skills/arch-design-doc.md
│   ├── schemas/
│   └── …（需求/契约/拍板）
├── engine/
│   ├── pipeline.js            # 权威入口 searchPlans
│   ├── mct.js
│   ├── score.js
│   ├── adapters/
│   │   ├── index.js           # train∥flight
│   │   ├── train.mock.js
│   │   ├── flight.mock.js
│   │   └── README.md
│   ├── export-*.js
│   ├── *-demo.js
│   └── README.md
└── miniprogram/               # 正式前端
    ├── app.js / app.json / app.wxss
    ├── package.json           # @vant/weapp
    ├── utils/ adapt-plans.js, plans.js
    ├── data/ plans-*.json
    └── pages/ query|results|detail|about
```

### 10.4 术语表

| 术语 | 定义 |
|------|------|
| 邪修交通 | 本产品：推荐比直达更聪明的空铁/高铁组合，只荐不卖 |
| 三主卡 | 最省钱 / 最快 / 最综合 三张主方案卡 |
| 怎么去 | 分段时间轴（腿 + 换乘节点） |
| 为什么 | 相对直达的人话解释（模板 + why_facts） |
| 怎么玩 | 换乘窗口达标时的枢纽 POI 建议 |
| Leg | 单段交通腿（火车或航班） |
| TransferPlay | 枢纽玩法 POI 记录 |
| MCT | Minimum Connection Time，最小换乘时间 |
| HUBS_MVP | MVP 头部 12 枢纽城列表 |
| path_mode | `auto` 自动选枢纽 / `user` 按途经 |
| vias | 有序途经城市，≤3 |
| pipeline | `engine/pipeline.js` 端到端编排 |
| score | 纯打分模块 |
| Adapter | 票源适配器，输出 Leg[] |
| train.mock / flight.mock | 火车/航班 mock 适配器 |
| source / as_of | 数据来源标识与快照时间 |
| price_ref_cny | 打分用参考价（元整数） |
| service_ref | 车次/航班号参考，引擎不臆造 |
| comfort | 席别舒适枚举 |
| pickMainCards | 挑选三主卡函数 |
| PlansSearchRequest/Response | API 契约出入参 |
| FE-Mini | 微信小程序正式前端 |
| FE-Proto | 冻结的 Web 原型 |
| ENG-Pipe / ENG-Rule | 流水线 / 纯规则模块类型 |
| ADP-Mock / ADP-Vendor | Mock / 合规源适配器类型 |
| LLM-Optional | 可选润色层，禁编班次票价 |
| 只荐不卖 | 不提供本站出票与收款 |
| 参考价 | 非实时余票价，以购票平台为准 |
| 同站 / 同城换乘 | MCT 分类 kind |
| 门到门时长 | 各段 duration + 正缓冲之和 |
| 屁股友好 | 产品口语：减少久坐硬座等不适（综合分代理） |
| 降级阶梯 | 真源→缓存→mock→错误 |
| 权威时序图 | §3.15，口径冲突时以该图为准 |
| 阻断项 | 上线前未完成则禁止发布的检查项 |
| 冻结原型 | index.html 不再作为生产演进主线 |

### 10.5 文档修订

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-23 | 首版：按 arch-design-doc 七律十章，对齐现仓代码与已拍板事实 |

---

*本文若与 `docs/技术与设计拍板.md` / PRD 冲突：产品红线与拍板优先；若与代码冲突：以 `engine/pipeline.js` + Schema 行为为准并回改文档。*
