# prototype/ — 静态 Web 示意原型（**FROZEN**）

本目录为历史可点击 HTML/CSS/JS 原型，**已冻结**：

- **禁止**再加功能、改交互、扩 OD、接真 API。
- 仅允许仓库级路径/文档同步所需的最小改动。

内容：`index.html`、`css/`、`js/`。方案 mock 现位于 [`../backend/data/`](../backend/data/)。本地预览需在**仓库根**起静态服务（以便相对路径 `../backend/data/...` 可解析），或自行改本地映射；正式客户端请用 `frontend/miniprogram` / 未来 `frontend/app`。

产品红线不变：**只推荐路线，不卖票。**
