# 演示点击路径（约 1 分钟）

面向 stakeholder 的最短走查。UI 为 **Vant 4** CDN 原型。

样例 OD（静态 mock，价格时刻均为**参考**）：

| OD | 数据文件 | 说明 |
|----|----------|------|
| **徐州 → 拉萨** | `data/mock/plans-xuzhou-lhasa.json` | 默认预填；长途邪修（直达硬座 / 空铁 / 西宁进藏） |
| **上海 → 成都** | `data/mock/plans-shanghai-chengdu.json` | 较短国内；经武汉最省钱 / 经西安最快 / 高铁直达综合；可途经武汉 |

查询页「样例」按钮可一键切换。

## 路径 A：默认自动邪修（徐州 → 拉萨）

1. 打开 `index.html` → **查询页**（已预填 徐州 → 拉萨，「日期灵活」已开）。
2. 点 **「开始邪修」** → 约 0.7 秒 **「正在邪修组合…」** → **结果页**。
3. 浏览三主卡：**最省钱** → **最快** → **最综合**。
4. 点开 **「最综合」** → **方案详情**：怎么去 / 为什么 / 怎么玩。
5. 点某段 **「去购票」** → Toast：只推荐不卖票。
6. 返回后可点「更多方案」；右上角 **「关于」** 看免责声明。

## 路径 A2：较短国内（上海 → 成都）

1. 查询页点样例 **「上海→成都」**（或手填），再 **「开始邪修」**。
2. 三主卡：**最省钱**（经武汉，含过夜段参考）→ **最快**（经西安高铁参考）→ **最综合**（高铁直达参考）。
3. 可选：添加途经 **武汉** → 切换「按你的路径」看分段高铁。

## 路径 B：指定途经西宁（徐州 → 拉萨）

1. 查询页点 **「添加途经城市」**（默认带出西宁）；确认 OD 为徐州→拉萨。
2. **「开始邪修」** → 路径条 + **按你的路径 / 系统自动邪修** 切换。
3. 对比两种模式后进详情。

## 路径 C：空态 / 失败（约 30 秒）

查询页底部小字演示开关（**默认关**）。文案见 [`docs/空态与文案规范.md`](docs/空态与文案规范.md)。

1. **校验 Toast**（失败一律留在查询页）
   - 清空出发或到达 → Toast「先填出发地和目的地」。
   - 出发=到达 → 「出发和到达不能是同一个地方」。
   - 途经加满 3 个后再点添加 → 按钮**禁用**；若仍触发 → 「途经最多 3 个」。
   - 途经写成与出发/到达相同 → 「途经别和出发/到达重复」。
2. **加载中** → 校验通过后进结果页，约 0.7 秒「正在邪修组合…」（主卡与更多方案此时不展示）。
3. **演示：无方案** → 勾选后「开始邪修」→ 空态标题「这趟暂时拼不出方案」+ 原因说明（含「不保证有票」）→ 「看看示例：徐州→拉萨」或「上海→成都」关开关并出结果；或「改查询条件」回查询。
4. **演示：加载失败** → 标题「这趟没查到，多半是数据暂不可用」+ 副文强调参考价、可改条件 → 「再试一次」仍可失败；「改条件」回查询。
5. **无效方案**（可选）→ 详情未知 id → Toast「方案找不到了，先回结果看看」。
